load("//:defs.bzl", "cx_rust_crate")

cx_rust_crate(
    name = "goal",
    compile_data = glob([
        "templates/**",
    ]),
    crate_name = "cx_goal_extension",
    integration_compile_data_extra = ["src/accounting.rs"],
)
