load("//:defs.bzl", "MACOS_WEBRTC_RUSTC_LINK_FLAGS", "cx_rust_crate")

cx_rust_crate(
    name = "tui",
    compile_data = glob(
        include = ["**"],
        allow_empty = True,
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
    ) + [
        "//cx-rs/collaboration-mode-templates:templates/default.md",
        "//cx-rs/collaboration-mode-templates:templates/plan.md",
    ],
    crate_name = "cx_tui",
    extra_binaries = [
        "//cx-rs/cli:cx",
    ],
    integration_compile_data_extra = ["src/test_backend.rs"],
    rustc_flags_extra = MACOS_WEBRTC_RUSTC_LINK_FLAGS,
    test_data_extra = glob([
        "src/**/*.rs",
        "src/**/snapshots/**",
    ]),
    test_shard_counts = {
        "tui-unit-tests": 8,
    },
)
