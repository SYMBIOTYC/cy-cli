[package]
name = "cx-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single CX workspace dependency.
# Add new CX surface area to `cx-core-api` instead of depending on
# additional `cx-*` crates here.
cx-core-api = { workspace = true }
tracing = { workspace = true }
